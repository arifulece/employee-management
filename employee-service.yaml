apiVersion: v1
kind: Service

metadata:
  name: employee-service
  namespace: employee-system
  labels:
    app: employee-app

spec:
  type: ClusterIP

  selector:
    app: employee-app

  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
